# SSP Assets

Drop SSP product assets here.

Required structure:
- logo: `/public/products/ssp/logo.svg`
- full backgrounds: `/public/products/ssp/backgrounds/*.png`
- thumbnails: `/public/products/ssp/backgrounds/thumbnails/*.jpg`

Then update `lib/products.ts` under `SSP_BACKGROUNDS` with the file names.
